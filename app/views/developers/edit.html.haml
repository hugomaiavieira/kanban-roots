%h1.heading
  = "Editing developer %s" % resource.id
  
- semantic_form_for(resource) do |form|
  = render 'form', :form => form
  - form.buttons do
    = form.commit_button 'Update'
    
%p.actions
  = link_to 'Cancel', collection_path
