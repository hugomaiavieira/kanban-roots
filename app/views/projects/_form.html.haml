- form.inputs do
  = form.input :name, :label => t('project.name')
  = form.input :description, :label => t('project.description')
  = form.input :start, :label => t('project.start'), :as => :string
  = form.input :end, :label => t('project.end'), :as => :string

